server:
  port: 8888
spring:
  cloud:
    config:
      server:
        default-label: main
        git:
          uri: https://github.com/mxfmd/microservices-lab.git
          search-paths: config-data
