server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mxfmd/microservices-lab.git
          search-paths: config-data
  application:
    name: config-service
